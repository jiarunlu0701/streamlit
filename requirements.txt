streamlit==1.13.0  # Replace with your desired version
openai==0.23.0     # Replace with the version compatible with your code
Pillow==9.1.0      # Replace with your desired version
sqlite3            # This is included in the standard Python library, no need to specify a version
